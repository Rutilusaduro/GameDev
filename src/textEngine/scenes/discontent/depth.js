// The Squad — Lead: A2 Psych | Support: A5 Editor
// Wildcard depth for discontent refusal pool (Pass 30).
import { registerModuleVariants } from '../../engine.js';

registerModuleVariants('discontent.refuse', [{ when: {}, text: [
  `{subject.name} slides the plate away untouched. "Not from you. Not tonight."`,
] }]);
