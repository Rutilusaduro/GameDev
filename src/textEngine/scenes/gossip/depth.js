// The Squad — Lead: C1 Gossip | Support: A5 Editor
// Wildcard depth for gossip react pools (Pass 30).
import { registerModuleVariants } from '../../engine.js';

registerModuleVariants('gossip.react.notice', [{ when: {}, text: [
  'The change registered — not announced, just absorbed into how the room looks at her.',
] }]);
